import NavBar from "../navbar";
import TitleContainer from "./styled";

const TitleScreen = () => {
  return (
    <TitleContainer>
      <NavBar />
    </TitleContainer>
  );
};

export default TitleScreen;
